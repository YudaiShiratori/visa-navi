{
  "description": "Claude Code MCP Configuration for Next.js Template",
  "servers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/workspace/src"],
      "description": "File system access for source code"
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "mcp-server-fetch"],
      "description": "Web content fetching for documentation and resources"
    }
  }
}